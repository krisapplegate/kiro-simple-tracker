# Location Tracker Environment Configuration

# ===========================================
# Backend Configuration
# ===========================================

# JWT Secret Key (CHANGE THIS IN PRODUCTION!)
JWT_SECRET=your-very-secure-secret-key-change-this-in-production

# Server Configuration
NODE_ENV=development
PORT=3001

# ===========================================
# Database Configuration
# ===========================================

# PostgreSQL Connection
DB_HOST=localhost
DB_PORT=5432
DB_NAME=location_tracker
DB_USER=tracker_user
DB_PASSWORD=tracker_password

# ===========================================
# MinIO Object Storage Configuration
# ===========================================

# MinIO Connection
MINIO_ENDPOINT=localhost
MINIO_PORT=9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=location-images

# ===========================================
# Docker Configuration
# ===========================================

# Database Password for Docker Compose
# (Used in docker-compose.yml and docker-compose.prod.yml)
DB_PASSWORD=tracker_password

# ===========================================
# Frontend Configuration (Vite)
# ===========================================

# API URL for frontend (used by Vite)
VITE_API_URL=http://localhost:3001

# ===========================================
# Production Configuration
# ===========================================

# For production deployment, ensure you:
# 1. Generate a strong JWT_SECRET (32+ characters)
# 2. Use a secure DB_PASSWORD
# 3. Set NODE_ENV=production
# 4. Update VITE_API_URL to your production backend URL

# Example production values:
# JWT_SECRET=super-secure-random-string-with-32-plus-characters
# DB_PASSWORD=very-secure-database-password-123
# NODE_ENV=production
# VITE_API_URL=https://your-backend-domain.com